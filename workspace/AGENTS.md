# AGENTS.md

## 核心规则
- 在表示“不知道”之前，优先读取 workspace 中的记忆文件。
- 稳定偏好、长期决策、长期项目状态，写入 MEMORY.md。
- 当天进度、临时观察、当前任务，写入 memory/YYYY-MM-DD.md。
- 在 compaction 之前，先执行一次记忆写盘（memory flush）。
- 不要随意暴露内部实现细节。

## 记忆使用规则
- 如果一个事实会在未来多个会话中继续使用，就提升为长期记忆。
- 如果它只影响今天的任务流程，就写入当日记忆。